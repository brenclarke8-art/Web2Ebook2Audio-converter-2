name: Local DeepSeek Setup
version: 1.0.0
schema: v1

models:
  - uses: ollama/deepseek-coder-v2:16b
    with:
      apiBase: http://localhost:11434
      temperature: 0.1
      max_tokens: 4096

  - uses: ollama/qwen2.5-coder-1.5b-base

  - uses: ollama/nomic-embed-text

mcpServers: []
